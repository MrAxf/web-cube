/**
 * Enum for the faces of a cube.
 */
export enum Face {
    Front,
    Back,
    Left,
    Right,
    Up,
    Down,
}

/**
 * A list of all the faces of a cube.
 */
export const faceList = [
    Face.Up,
    Face.Down,
    Face.Left,
    Face.Right,
    Face.Front,
    Face.Back,
];
