{
  "name": "web-cube-root",
  "version": "1.0.0",
  "workspaces": [
    "./packages/web-cube",
    "./docs"
  ],
  "scripts": {
    "dev": "npm run --workspace=web-cube dev",
    "build": "npm run --workspace=web-cube build"
  }
}